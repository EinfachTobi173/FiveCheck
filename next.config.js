/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // Keep defaults; can enable serverActions later if needed
  }
};

module.exports = nextConfig;